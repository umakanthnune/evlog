{
  "name": "uklabs-ev-trip-log-api",
  "version": "1.0.0",
  "description": "Backend API for UKLabs EV Trip Log",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node -e \"require('./db').query(require('fs').readFileSync('./schema.sql','utf8')).then(()=>{console.log('migrated');process.exit(0)}).catch(e=>{console.error(e);process.exit(1)})\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5",
    "sharp": "^0.33.4"
  }
}
